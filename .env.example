# ──────────────────────────────────────────────────────────────────
# Donation Platform — Frontend Environment Variables
# Copy this file to .env (for local dev) or .env.production (for builds).
# ──────────────────────────────────────────────────────────────────

# Backend API base URL (no trailing slash)
# Development : leave empty — Vite dev-server proxy handles routing
# Production  : set to your deployed backend, e.g.
#   VITE_API_URL=https://donation-platform-api.onrender.com
VITE_API_URL=
